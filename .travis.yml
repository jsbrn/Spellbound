language: java
dist: trusty
jdk: oraclejdk8
script: mvn package
deploy:
  provider: releases
  api_key: "$GITHUB_AUTH_TOKEN"
  file: "target/Spellbound.jar"
  name: "Spellbound-$TRAVIS_TAG"
  prerelease: true
  skip_cleanup: true
  on:
    tags: true